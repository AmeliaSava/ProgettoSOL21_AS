Relazione Progetto Amelia Sava Corso A 2021/2022

Server

1.File di Configurazione
Nel main si fa il parsing del file config.txt, il formato del file config.txt prevede (la possibilità di commentare con #) di scrivere gli argomenti uno per riga e si suppone che l'ordine viene rispettato, le righe devono essere terminate da ; e il nome della variabile separato dall'uguale, possono esserci spazi.

Si è deciso di utilizzare una struttura dati di tipo 
I file con stato 1 sono chiusi, con stato 0 sono aperti

Il file ops.h si basa sul file util.h fornito a lezione.

Oltre al thread master main è presente un thread signal handler

Memoria cache
Per rappresentare la memoria cache si è utilizzata una struttura dati hash, con politica di rimozione LFU (dire come implementata)

Client

Quando vengono passati i file si suppon che venga passato il path assoluto del file.

isdot cwd e write_from_dir_find sono basate su funzioni viste a lezione.

il client prevede un socket nella sua directory corrente

API

OpenFile ritorna un risultato aggiuntivo 1, per implementare il controllo sulla write file successiva
se -w fallisce a scrivere un file passa al prossimo per completare la quantita di files richiesta